#include "HOWL.h"

// Voiceline_EmptyFunc @ 0x8002D2A8 (SCUS_944.26.exe, 926/NTSC-U). SCORE-0 MATCH.
// Empty stub — the retail function is just `jr $ra; nop` (does nothing). Called from
// func_8002D2A8's caller cluster (jal @0x8002D4E8). Likely a debug/no-op hook.

void Voiceline_EmptyFunc(void)
{
}
